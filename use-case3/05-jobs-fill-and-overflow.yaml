apiVersion: batch/v1
kind: Job
metadata:
  name: dev-medium-priority
  namespace: training-jobs
  labels:
    kueue.x-k8s.io/queue-name: priority-queue
    kueue.x-k8s.io/priority-class: medium-priority
spec:
  suspend: true
  template:
    metadata:
    spec:
      restartPolicy: Never
      containers:
      - name: training-workload
        image: registry.access.redhat.com/ubi9/ubi
        command: ["/bin/bash", "-c"]
        args:
            - |
              import torch, time, socket
              mem = torch.cuda.get_device_properties(0).total_memory / 1e9
              print(f"[alice-dev] MEDIUM-PRIORITY experiment | Node: {socket.gethostname()} | VRAM: {mem:.1f}GB")
              print("[alice-dev] Running dev experiment — will be preempted by production job")
              x = torch.randn(1024, 1024, device='cuda')
              for i in range(360):
                  _ = torch.mm(x, x)
                  time.sleep(5)
        resources:
          requests:
            cpu: "2"
            memory: "4Gi"
            nvidia.com/gpu: "2"                    
          limits:
            nvidia.com/gpu: "2"