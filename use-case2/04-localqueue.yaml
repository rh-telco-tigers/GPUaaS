apiVersion: kueue.x-k8s.io/v1beta2
kind: LocalQueue
metadata:
  name: inference-queue
  namespace: gpuaas-demo
  annotations:
    description: "Team submit point — routes to fifo-gpu-queue-3 (3 GPU FIFO)"
spec:
  clusterQueue: t4-queue
